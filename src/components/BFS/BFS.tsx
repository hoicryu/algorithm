import React, { useState } from "react";

const BFS: React.FC = () => {
  return (
    <div className="mt-10 flex flex-col items-center">
      <h2>1. 너비우선탐색</h2>
    </div>
  );
};

export default BFS;
